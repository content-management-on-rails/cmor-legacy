= Slick::Rails::ApplicationViewHelper.new(self).render_carousel(options) do
  - capture_haml do
    - category.partners.published.each do |partner|
      %div
        %a{ href: url_for(partner) }
          %img{ src: main_app.url_for(partner.logo.variant(resize: "128x64>", gravity: :center, extent: "128x64")) }/