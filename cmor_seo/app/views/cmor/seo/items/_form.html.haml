= form.input :resource, as: :polymorphic_association, classes: Cmor::Seo::Configuration.items_resource_autocomplete_options.each_with_object({}) { |(k, v), m| m[k.constantize] = url_for(action: :autocomplete_resource, resource_class_name: k) }
= form.input :path
= form.input :published, as: :boolean

.row
  .col-12
    %h3= Cmor::Seo::MetaTag.model_name.human(count: :other)

.row
  .col-12
    #meta_tags
      .mb-2
      = form.simple_fields_for :meta_tags do |mt_form|
        = render partial: 'meta_tag_fields', locals: { f: mt_form }
      .links.mb-2
        = link_to_add_association t('.add_association'), form, :meta_tags, class: 'btn btn-success'

.row
  .col-12
    %hr