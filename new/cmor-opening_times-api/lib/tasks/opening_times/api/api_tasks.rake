namespace :cmor do
  namespace :opening_times do
    namespace :api do
      # desc "example task"
      # task :example, [:arg_1, :arg_2] => [:environment] do |_t, args|
      # end
    end
  end
end
